---
---

<section class="cta section">
  <div class="container">
    <div class="cta-content">
      <h2 class="cta-title">Let's create and attend Something Amazing Together</h2>
      <p class="cta-description">
        Join our community of passionate event creators and attendees who are making every gathering special. 
        We're here to help you succeed, every step of the way.
      </p>
      <div class="cta-actions">
        <a href="/register" class="btn btn-primary btn-large">Get Started for Free</a>
        <a href="#contact" class="btn btn-secondary btn-large">Chat with Us</a>
      </div>
    </div>
  </div>
</section>

<style>
  .cta {
    background: #f9fafb;
    position: relative;
    overflow: hidden;
  }

  .cta-content {
    text-align: center;
    position: relative;
    z-index: 1;
    max-width: 800px;
    margin: 0 auto;
    padding: 2rem 0;
  }

  .cta-title {
    font-size: 2.5rem;
    font-weight: 700;
    color: #1f2937;
    margin-bottom: 1rem;
  }

  .cta-description {
    font-size: 1.25rem;
    color: #6b7280;
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .cta-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-large {
    padding: 1rem 2.5rem;
    font-size: 1.125rem;
    border-radius: 8px;
    text-transform: none;
    letter-spacing: 0;
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
  }

  .cta-actions .btn-primary {
    background: linear-gradient(135deg,rgb(20, 129, 184), rgb(20, 167, 190));
    color: white;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  }

  .cta-actions .btn-primary:hover {
    background: linear-gradient(135deg,rgb(13, 94, 148), rgb(20, 167, 190));
    transform: translateY(-2px);
    box-shadow: 0 6px 12px -2px rgba(0, 0, 0, 0.15);
  }

  .cta-actions .btn-secondary {
    background: white;
    color:rgb(20, 129, 184);
    border: 2px solidrgb(20, 129, 184);
  }

  .cta-actions .btn-secondary:hover {
    background:rgb(20, 132, 184);
    color: white;
  }

  @media (max-width: 768px) {
    .cta-title {
      font-size: 2rem;
    }

    .cta-description {
      font-size: 1.125rem;
    }

    .cta-actions {
      flex-direction: column;
      align-items: center;
    }

    .cta-actions .btn {
      width: 100%;
      max-width: 300px;
    }
  }
</style>
