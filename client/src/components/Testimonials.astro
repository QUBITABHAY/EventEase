---
const testimonials = [
  {
    name: "Aditya",
    role: "Event Manager, TechCorp",
    image: "",
    rating: 5,
    text: "EventEase has completely transformed how we manage our corporate events. The analytics dashboard is incredibly insightful!"
  },
  {
    name: "Abhay",
    role: "University Event Coordinator",
    image: "",
    rating: 5,
    text: "Managing university events has never been easier. The platform is intuitive and our students love the registration process."
  },
  {
    name: "Yashveer",
    role: "Community Organizer",
    image: "",
    rating: 5,
    text: "As a freelancer, I needed an affordable solution. EventEase provides enterprise features at a fraction of the cost!"
  }
];
---

<section class="testimonials section">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">What Our Users Say</h2>
      <p class="section-description">
        Trusted by thousands of event organizers worldwide
      </p>
    </div>

    <div class="testimonials-grid">
      {testimonials.map((testimonial) => (
        <div class="testimonial-card">
          <div class="testimonial-header">
            <div class="testimonial-info">
              <h3 class="testimonial-name">{testimonial.name}</h3>
              <p class="testimonial-role">{testimonial.role}</p>
            </div>
          </div>
          <div class="testimonial-rating">
            {Array.from({ length: testimonial.rating }).map(() => (
              <span class="star">★</span>
            ))}
          </div>
          <p class="testimonial-text">"{testimonial.text}"</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .testimonial-card {
    background: white;
    padding: 2rem;
    border-radius: 0;
    box-shadow: var(--shadow-md);
    transition: all 0.3s ease;
  }

  .testimonial-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-xl);
  }

  .testimonial-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .testimonial-avatar {
    display: none;
  }

  .testimonial-info {
    flex: 1;
  }

  .testimonial-name {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--text-dark);
    margin-bottom: 0.25rem;
  }

  .testimonial-role {
    font-size: 0.875rem;
    color: var(--text-light);
  }

  .testimonial-rating {
    display: flex;
    gap: 0.25rem;
    margin-bottom: 1rem;
  }

  .star {
    font-size: 1.25rem;
    color: #fbbf24;
  }

  .testimonial-text {
    color: var(--text-dark);
    line-height: 1.6;
    font-style: italic;
  }

  @media (max-width: 768px) {
    .testimonials-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
